// Auto-converted from /tmp/df-hyper-1.9.0/hyper-1.9.0/src/common/either.rs

use pin_project_lite::pin_project;
use std::{ future::Future, pin::Pin, task::{ Context, Poll } };
pin_project ! { #[doc = " One of two possible futures that have the same output type."] #[project = EitherProj] pub(crate) enum Either<F1, F2> { Left { #[pin] fut: F1 }, Right { #[pin] fut: F2 } } }
impl<F1, F2> Either<F1, F2> {
 fn left(fut: F1) -> Self {
 Either::Left { fut }
 }
 
 fn right(fut: F2) -> Self {
 Either::Right { fut }
 }
 
}

impl<F1, F2> Future for Either<F1, F2> where F1: Future, F2: Future<_> {
 type Output = F1::Output;
 fn poll(self, cx: &mut Context<'_>) -> Poll<Self::Output> {
 match self.project() { EitherProj::Left { fut } => fut.poll(cx), EitherProj::Right { fut } => fut.poll(cx) }
 }
 
}

